services:
  lightrag-ws1:
    container_name: lightrag-ws1
    build:
      context: .
      dockerfile: Dockerfile
    image: lightrag-local:latest
    volumes:
      - lightrag_ws1_storage:/app/data/rag_storage
      - lightrag_ws1_inputs:/app/data/inputs
      - ./config.ini:/app/config.ini
      - ./.env:/app/.env
    env_file:
      - .env
    environment:
      - WORKSPACE=ws1
      - PORT=9621
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - dokploy-network

  lightrag-unireformada:
    container_name: lightrag-unireformada
    build:
      context: .
      dockerfile: Dockerfile
    image: lightrag-local:latest
    volumes:
      - lightrag_unireformada_storage:/app/data/rag_storage
      - lightrag_unireformada_inputs:/app/data/inputs
      - ./config.ini:/app/config.ini
      - ./.env:/app/.env
    env_file:
      - .env
    environment:
      - WORKSPACE=unireformada
      - PORT=9621
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - dokploy-network

  lightrag-museo:
    container_name: lightrag-museo
    build:
      context: .
      dockerfile: Dockerfile
    image: lightrag-local:latest
    volumes:
      - lightrag_museo_storage:/app/data/rag_storage
      - lightrag_museo_inputs:/app/data/inputs
      - ./config.ini:/app/config.ini
      - ./.env:/app/.env
    env_file:
      - .env
    environment:
      - WORKSPACE=museo
      - PORT=9621
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - dokploy-network

  lightrag-barq-art:
    container_name: lightrag-barq-art
    build:
      context: .
      dockerfile: Dockerfile
    image: lightrag-local:latest
    volumes:
      - lightrag_barq_art_storage:/app/data/rag_storage
      - lightrag_barq_art_inputs:/app/data/inputs
      - ./config.ini:/app/config.ini
      - ./.env:/app/.env
    env_file:
      - .env
    environment:
      - WORKSPACE=barq-art
      - PORT=9621
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - dokploy-network
      
  lightrag-tucan:
    container_name: lightrag-tucan
    build:
      context: .
      dockerfile: Dockerfile
    image: lightrag-local:latest
    volumes:
      - lightrag_tucan_storage:/app/data/rag_storage
      - lightrag_tucan_inputs:/app/data/inputs
      - ./config.ini:/app/config.ini
      - ./.env:/app/.env
    env_file:
      - .env
    environment:
      - WORKSPACE=tucan
      - PORT=9621
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - dokploy-network

  # --- NUEVO SERVICIO: evolver-your-english ---
  lightrag-evolver-your-english:
    container_name: lightrag-evolver-your-english
    build:
      context: .
      dockerfile: Dockerfile
    image: lightrag-local:latest
    volumes:
      - lightrag_evolver_your_english_storage:/app/data/rag_storage
      - lightrag_evolver_your_english_inputs:/app/data/inputs
      - ./config.ini:/app/config.ini
      - ./.env:/app/.env
    env_file:
      - .env
    environment:
      - WORKSPACE=evolver-your-english
      - PORT=9621
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - dokploy-network

  # --- NUEVO SERVICIO: iditek ---
  lightrag-iditek:
    container_name: lightrag-iditek
    build:
      context: .
      dockerfile: Dockerfile
    image: lightrag-local:latest
    volumes:
      - lightrag_iditek_storage:/app/data/rag_storage
      - lightrag_iditek_inputs:/app/data/inputs
      - ./config.ini:/app/config.ini
      - ./.env:/app/.env
    env_file:
      - .env
    environment:
      - WORKSPACE=iditek
      - PORT=9621
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true

volumes:
  lightrag_ws1_storage:
    driver: local
  lightrag_ws1_inputs:
    driver: local

  lightrag_unireformada_storage:
    driver: local
  lightrag_unireformada_inputs:
    driver: local
  
  lightrag_museo_storage:
    driver: local
  lightrag_museo_inputs:
    driver: local

  lightrag_barq_art_storage:
    driver: local
  lightrag_barq_art_inputs:
    driver: local

  lightrag_tucan_storage:
    driver: local
  lightrag_tucan_inputs:
    driver: local

  # --- NUEVOS VOLUMENES ---
  lightrag_evolver_your_english_storage:
    driver: local
  lightrag_evolver_your_english_inputs:
    driver: local

  lightrag_iditek_storage:
    driver: local
  lightrag_iditek_inputs:
    driver: local
